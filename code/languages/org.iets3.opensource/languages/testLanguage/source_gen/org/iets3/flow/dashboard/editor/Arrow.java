package org.iets3.flow.dashboard.editor;

/*Generated by MPS */

import de.itemis.mps.editor.diagram.runtime.shape.AbstractShape;
import org.jetbrains.annotations.Nullable;
import java.awt.Graphics2D;
import java.awt.geom.Rectangle2D;
import de.itemis.mps.editor.diagram.runtime.shape.IShapeStyle;
import java.awt.geom.GeneralPath;

public class Arrow extends AbstractShape {

  private double relativeHeight;
  private boolean filled;
  private double relativeWidth;
  private int status;

  public Arrow(double relativeHeight, boolean filled, double relativeWidth, int status) {
    this.relativeHeight = relativeHeight;
    this.filled = filled;
    this.relativeWidth = relativeWidth;
    this.status = status;
  }

  @Nullable
  @Override
  public Double getDefaultSize() {
    return ((double) (15));
  }



  @Override
  protected void draw(final Graphics2D graphics, final Rectangle2D bounds, IShapeStyle style) {

    GeneralPath arrowShape = new GeneralPath();
    double width = bounds.getWidth();
    double height = bounds.getHeight();

    double x2 = bounds.getMaxX();
    double x = x2 - width * this.relativeWidth;


    double y = bounds.getY();
    double y2;
    double centerY = y + height / 2;

    arrowShape.moveTo(x, centerY - height * this.relativeHeight);
    arrowShape.lineTo(x2, centerY);
    arrowShape.lineTo(x, centerY + height * this.relativeHeight);

    if (this.filled) {
      arrowShape.closePath();
    }
    if (this.filled) {
      graphics.fill(arrowShape);
    } else {
      graphics.draw(arrowShape);
    }
  }

}
